# Auto-generated by changesets — leave as-is so the changeset bot
# isn't forced to round-trip through our formatter on every release PR.
**/CHANGELOG.md

# Build artefacts
dist/
.turbo/
node_modules/

# Generated by `pnpm build:schema` — keep as the generator emits it so the
# committed file stays byte-identical to its source (no formatter round-trip).
packages/website/public/schema/config.json

# Lockfiles handled by their own tooling
pnpm-lock.yaml

# Vendored agent playbook + per-rule fix recipes (pinned fork). Written by
# `scripts/vendor-prompts.mjs` as a reviewed upstream snapshot (repointed to
# the fork); kept byte-for-byte so re-vendoring needs no formatter round-trip.
prompts/
